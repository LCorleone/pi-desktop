name: Build (macOS)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-macos:
    name: Build macOS (Intel x86_64)
    # Run on Apple Silicon runner (fast provisioning) and cross-compile for
    # Intel so the resulting .dmg runs on x86_64 Macs.
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust (with x86_64-apple-darwin target)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Install dependencies
        run: npm ci

      - name: Build app (cross-compile for Intel)
        run: npm run tauri build -- --target x86_64-apple-darwin

      - name: Upload macOS bundle
        uses: actions/upload-artifact@v4
        with:
          name: Pi-Desktop-macos-x86_64
          path: |
            src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg
          if-no-files-found: error

      - name: Unsigned build notice
        run: |
          echo "## macOS build ready (unsigned)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Download the artifact, mount the .dmg, and drag **Pi Desktop** to Applications." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Because the app is unsigned, macOS will block first launch. Remove the quarantine attribute:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "xattr -cr \"/Applications/Pi Desktop.app\"" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
